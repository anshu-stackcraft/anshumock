import { Link,useParams } from 'react-router-dom'; import { Layout,Page } from './_layout'; export default function MockExam(){const{mockId}=useParams();return <Layout><Page title={`Mock Exam ${mockId}`} text="Read each question carefully."><div className="panel question-card"><h2>Mock test instructions</h2><p>100 questions, 120 minutes and negative marking.</p><Link className="orange-btn" to={`/result/${mockId}`}>Submit test</Link></div></Page></Layout>}
