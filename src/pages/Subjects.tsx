import { Link,useParams } from 'react-router-dom'; import { Layout,Page } from './_layout'; export default function Subjects(){const{examId}=useParams();return <Layout><Page title="Subjects" text={`Practice subjects for ${examId}`}><div className="subject-grid large">{['History','Geography','Polity','Science','Maths','Reasoning'].map(x=><Link className="subject-card" to={`/practice/${x.toLowerCase()}`} key={x}><b>{x}</b><span>124 questions</span></Link>)}</div></Page></Layout>}
